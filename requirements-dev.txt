# ── Test-only dependencies ────────────────────────────────────────────────
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Inside the running container (which has requirements.txt already):
#   docker exec promptiq-web pip install -r /app/requirements-dev.txt
#
# NOTE: a `docker compose build` does not pick these up — requirements-dev.txt
# is deliberately not in the Dockerfile so production images stay lean. After a
# rebuild, re-run the pip install above.

pytest>=8.0.0
# 0.24 introduced asyncio_default_test_loop_scope, which pytest.ini relies on to
# share one event loop across the session-scoped engine and every test.
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
# Deterministic ordering + the -p no:randomly style guards; also gives -n for
# local speed. Integration tests share one DB, so only use -n with care.
pytest-xdist>=3.5.0

# httpx is already a runtime dependency (Mistral calls), pinned here too because
# ASGITransport is what drives the API tests.
httpx>=0.24.0

# Contract/fuzz testing against the live OpenAPI schema.
schemathesis>=3.25.0
